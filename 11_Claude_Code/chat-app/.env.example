# Absolute path of the repository the concierge answers questions about.
TARGET_REPO=/Users/logan/myos/Projects/reforge

# Hard cap on agent loop iterations per request.
MAX_TURNS=25

# Optional. Omit to use your local Claude Code login; set it to bill via the API.
# ANTHROPIC_API_KEY=sk-ant-...
